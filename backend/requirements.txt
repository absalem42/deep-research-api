# Trimmed hard from the reference requirements.txt, which pulled azure-identity,
# azure-search, langchain-aws, pandas and pymupdf into every image without any
# of them being imported. Smaller image, smaller CVE surface, faster cold start.

# --- web layer ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
sse-starlette>=2.1.0

# --- agent graph ---
langchain>=1.3.15
langchain-core>=1.5.5
langgraph>=1.2.11
langchain-mcp-adapters>=0.3.2
mcp>=1.9.4

# --- providers (each optional at runtime; import is lazy) ---
langchain-openai>=1.5.1
langchain-anthropic>=1.5.6
langchain-groq>=1.1.3
langchain-google-genai>=4.3.4
langchain-deepseek>=1.1.0

# --- search / fetch ---
tavily-python>=0.5.0
aiohttp>=3.10.0

# --- shared job state (only needed when JOB_BACKEND=redis) ---
redis>=5.2.0

# --- ops ---
python-dotenv>=1.0.0
